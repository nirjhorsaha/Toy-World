import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddToys = () => {
    useTitle('Add Toys');
    return (
        <div>
            <h1>add Toys</h1>
        </div>
    );
};

export default AddToys;