import React from 'react';
import useTitle from '../../hooks/useTitle';

const SignIn = () => {
    useTitle('Login')
    return (
        <div>
            <h1>signin page</h1>
        </div>
    );
};

export default SignIn;